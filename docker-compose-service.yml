version: "2"
#
# Services
#
services:
   command:
      build: ./command/
      volumes:
        - ./docker/command/config:/config
        - ./docker/command/service:/service
      networks:
          - localnet


# Networks
#
networks:
  localnet:
    # driver: bridge
    external: true