version: "2"
#
# Services
#
services:
   command:
      build: ./command/
      volumes:
        - ./docker/command/config:/config
        - ./docker/command/service:/service
      ports:
        - 8083:8083
      networks:
          - localnet


# Networks
#
networks:
  localnet:
    # driver: bridge
    external: true