version: "2"
#
# Services
#
services:
   command:
      build: ./command/
      volumes:
        - ./docker/command/config:/config
      networks:
          - localnet


# Networks
#
networks:
  localnet:
    # driver: bridge
    external: true