version: "2"
#
# Services
#
services:
   cdcservice:
      build: ./cdc/
      volumes:
        - ./docker/cdc/config:/config
      networks:
          - localnet



# Networks
#
networks:
  localnet:
    # driver: bridge
    external: true